<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
  </style>

  <style name="AppTheme.Main" parent="AppTheme">
  </style>

  <style name="OneFrameworkTheme">
    <item name="android:background">@color/app_background</item>
  </style>

  <style name="MainTheme" parent="android:Theme.Light.NoTitleBar">
    <item name="android:windowAnimationStyle">@style/MainTheme.Anim</item>
  </style>

  <style name="MainTheme.Anim" parent="@android:style/Animation.Activity">
    <item name="android:activityOpenEnterAnimation">@android:anim/fade_in</item>
    <item name="android:activityOpenExitAnimation">@android:anim/fade_out</item>
    <item name="android:activityCloseEnterAnimation">@android:anim/fade_in</item>
    <item name="android:activityCloseExitAnimation">@android:anim/fade_out</item>
  </style>

  <style name="TextButtonStyle">
    <item name="android:layout_width">wrap_content</item>
    <item name="android:layout_height">match_parent</item>
    <item name="android:padding">10dp</item>
    <item name="android:clickable">true</item>
    <item name="android:textColor">@color/one_text_button_style</item>
  </style>

  <!-- pop up window 默认动画从底部出来 从底部出去-->
  <style name="PopDefaultAnimation">
    <item name="android:windowEnterAnimation">@anim/one_in_bottom_to_up</item>
    <item name="android:windowExitAnimation">@anim/one_out_up_to_bottom</item>
  </style>

  <style name="PopAlphaAnimation">
    <item name="android:windowEnterAnimation">@anim/one_pop_alpha_in</item>
    <item name="android:windowExitAnimation">@anim/one_pop_alpha_out</item>
  </style>

  <style name="TopBarMenuPopAnimation">
    <item name="android:windowEnterAnimation">@anim/one_fade_in</item>
    <item name="android:windowExitAnimation">@anim/one_fade_out</item>
  </style>

  <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
    <!-- 背景透明 -->
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowContentOverlay">@null</item>
    <!-- 浮于Activity之上 -->
    <item name="android:windowIsFloating">true</item>
    <!-- 边框 -->
    <item name="android:windowFrame">@null</item>
    <!-- Dialog以外的区域模糊效果 -->
    <item name="android:backgroundDimEnabled">true</item>
    <!-- 无标题 -->
    <item name="android:windowNoTitle">true</item>
    <!-- 半透明 -->
    <item name="android:windowIsTranslucent">true</item>
    <!-- Dialog进入及退出动画 -->
    <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
  </style>

  <!-- ActionSheet进出动画 -->
  <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
    <item name="android:windowEnterAnimation">@anim/one_actionsheet_dialog_in</item>
    <item name="android:windowExitAnimation">@anim/one_actionsheet_dialog_out</item>
  </style>

  <style name="ImageViewStyle">
    <item name="android:scaleType">fitXY</item>
    <item name="android:contentDescription">@null</item>
  </style>

  <style name="StarViewStyle" parent="ImageViewStyle">
    <item name="android:maxHeight">48dp</item>
    <item name="android:maxWidth">48dp</item>
    <item name="android:layout_width">wrap_content</item>
    <item name="android:layout_height">wrap_content</item>
    <item name="android:layout_marginRight">10dp</item>
  </style>

  <style name="PopWindowAnim" parent="android:Animation">
    <item name="android:windowEnterAnimation">@anim/one_pop_window_enter_anim</item>
    <item name="android:windowExitAnimation">@anim/one_pop_window_exit_anim</item>
  </style>

  <style name="OneToastAnim">
    <item name="android:activityOpenEnterAnimation">@anim/one_base_toast_show_dialog_anim</item>
    <item name="android:activityOpenExitAnimation">@anim/one_base_toast_dismiss_dialog_anim</item>
    <item name="android:activityCloseEnterAnimation">@anim/one_base_toast_show_dialog_anim</item>
    <item name="android:activityCloseExitAnimation">@anim/one_base_toast_dismiss_dialog_anim</item>
  </style>

  <style name="LoadingDialogTheme" parent="@android:style/Theme.Dialog">
    <item name="android:windowFrame">@null</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowBackground">@color/one_dialog_loading_bg_translate</item>
    <item name="android:windowIsFloating">false</item>
    <item name="android:windowContentOverlay">@null</item>
  </style>

  <style name="OneFrameworkTheme.NoTitle">
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:windowAnimationStyle">@style/Animation.Activity</item>
  </style>

  <style name="Animation.Activity" parent="@android:style/Animation.Activity">
    <item name="android:activityOpenEnterAnimation">@anim/right_slide_in</item>
    <item name="android:activityOpenExitAnimation">@anim/right_slide_out</item>
    <item name="android:activityCloseEnterAnimation">@anim/slide_back_in</item>
    <item name="android:activityCloseExitAnimation">@anim/slide_back_out</item>
  </style>
</resources>